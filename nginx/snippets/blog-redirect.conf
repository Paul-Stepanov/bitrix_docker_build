# Редирект для /blog и подпапок — добавляем слеш (query string сохраняется)
# Файл вынесен отдельно — подключайте в свой конфиг при необходимости:
#   include /etc/nginx/snippets/blog-redirect.conf;
location ~ "^/blog(?:/.*[^/])?$" {
    rewrite ^(.*)$ $1/ permanent;
}